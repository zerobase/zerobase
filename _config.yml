# Build settings
destination: public
exclude: [
  CNAME,
  Gemfile,
  Gemfile.lock,
  LICENSE,
  LICENSE_FOR_CONTENTS,
  LICENSE_FOR_SOFTWARE,
  README.md,
  package.json,
  package-lock.json,
  yarn.lock,
  node_modules,
  vendor,
  firebase.json
  ]

# Site settings
title: 石橋秀仁 (Hide Ishi)
#lead: "an information architect"
author: 石橋秀仁 (Hide Ishi)
thumbnail: /images/thumbnail.png
email: hidetoi@gmail.com
description: "an information architect / インフォメーション・アーキテクト"
baseurl: "" # the subpath of your site, e.g. /blog/
url: "https://ja.ishibashihideto.net" # the base hostname & protocol for your site
twitter_username: zerobase
github_username:  zerobase
main_feed: https://feeds.feedburner.com/HidetoIshibashi
podcast_feed: https://feeds.feedburner.com/InformationArchitectsPodcast
podcast_itunes_url: https://itunes.apple.com/jp/podcast/ia-podcast/id963102127?l=en&mt=2

## Custom settings
show_header_nickname: false
show_lang_switcher: false
show_facebook_comment: false

# Build settings
markdown: kramdown
highlighter: rouge
plugins:
  - jekyll-redirect-from
  - jekyll-sitemap
  - jekyll-linkpreview
  - jekyll-figure
  - jekyll-extlinks
extlinks:
  attributes: {target: _blank}
  # rel_exclude: ['host1.com', 'host2.net']

# Blog
permalink: date # previously: /blog/:categories/:title/

# Default variables

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      layout: "post"
      author: "石橋秀仁 (Hide Ishi)"
  -
    scope:
      path: "blog" # an empty string here means all files in the project
    values:
      categories: "blog"
  -
    scope:
      path: "activity" # an empty string here means all files in the project
    values:
      categories: "activity"
  -
    scope:
      path: "podcast" # an empty string here means all files in the project
    values:
      categories: "podcast"

# Twitter
twitter_user: zerobase
twitter_tweet_button: true

# Facebook
facebook_app_id: 422391257870402
facebook_locale: ja_JP

# Google Analytics
google_analytics_tracking_id: UA-41591703-2
